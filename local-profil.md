## Profil — Kevin

- **TDAH + fatigue exécutive** (séquelle burnout) : chaque mot lu / décision visuelle a un coût réel. C'est la raison de fond du style à charge cognitive minimale (voir `local-persona.md`).
- **Logique 20/80** : va à l'essentiel, une seule reco claire plutôt qu'un éventail d'options.
- **Concret > abstrait** : un exemple ou un cas d'usage avant la théorie.

## Préférences d'outils

- **Capture** : j'utilise `/vault:capture` (mon vault Obsidian gère la mémoire), **pas** `/capture` du memory-bank — d'où `_claudeTeam.memoryBank: "off"` dans `settings.local.json`.
